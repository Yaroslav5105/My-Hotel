@model room


<div class="col-lg-4">
    <img class="img-thumbnail" src="Model.img" alt="Model.name" />
    <h2>@Model.name</h2>
    <p>@Model.shortDesc</p>
    <p>Цена : @Model.price.ToString("С") </p>
    <p><a class="btn btn-warning" href="#">Подробно</a></p>
</div>